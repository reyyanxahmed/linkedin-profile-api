# linkedin-profile-api

HTTP API that returns a LinkedIn profile as structured JSON by hitting LinkedIn's
internal Voyager endpoints directly over HTTP. No browser anywhere in the runtime.

> Full documentation is written in Phase 5. This placeholder satisfies the build system.
> See `BUILD_SPEC.md` for the complete specification and `CLAUDE.md` for the working
> agreement under which this repo is built.